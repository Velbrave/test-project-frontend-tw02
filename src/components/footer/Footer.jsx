import React from 'react';
import css from './Footer.module.css';

const Footer = () => {
  return (
    <div className={css.footer}>
      <h2 className={css.title}>Footer</h2>
    </div>
  );
};

export default Footer;
